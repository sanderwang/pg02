---
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  namespace: "kubenuts"
  name: "external-secrets-helm-release"
spec:
  dependsOn:
  - namespace: "kubenuts"
    name: "external-secrets-helm-values"
  interval: 10m
  prune: true
  sourceRef:
    kind: GitRepository
    namespace: flux-system
    name: flux-system
  path: "/manifests/external-secrets/external-secrets-helm-release"
  targetNamespace: "kubenuts"
  healthChecks:
  - apiVersion: helm.toolkit.fluxcd.io/v2
    kind: HelmRelease
    namespace: "kubenuts"
    name: "external-secrets"
