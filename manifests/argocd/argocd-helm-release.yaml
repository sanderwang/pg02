---
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  namespace: "argocd"
  name: "argocd-helm-release"
spec:
  dependsOn:
  - namespace: "argocd"
    name: "argocd-helm-values"
  - namespace: "argocd"
    name: "argocd-secrets"
  interval: 10m
  prune: true
  sourceRef:
    kind: GitRepository
    namespace: flux-system
    name: flux-system
  path: "/manifests/argocd/argocd-helm-release"
  targetNamespace: "argocd"
  healthChecks:
  - apiVersion: helm.toolkit.fluxcd.io/v2
    kind: HelmRelease
    namespace: "argocd"
    name: "argocd"
