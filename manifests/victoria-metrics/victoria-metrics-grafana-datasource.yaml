---
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  namespace: "victoria-metrics"
  name: "victoria-metrics-grafana-datasource"
spec:
  interval: 10m
  prune: true
  sourceRef:
    kind: GitRepository
    namespace: flux-system
    name: flux-system
  path: "/manifests/victoria-metrics/victoria-metrics-grafana-datasource"
  targetNamespace: "victoria-metrics"
  healthChecks:
  - apiVersion: v1
    kind: ConfigMap
    namespace: "victoria-metrics"
    name: "victoria-metrics-grafana-datasource"
