---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: "polaris"
generatorOptions:
  disableNameSuffixHash: true
configMapGenerator:
- name: "polaris-helm-values"
  files:
  - values.yaml
