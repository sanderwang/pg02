---
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  namespace: "telemetry"
  name: "grafana-helm-release"
spec:
  dependsOn:
  - namespace: "telemetry"
    name: "grafana-helm-values"
  - namespace: "telemetry"
    name: "grafana-secrets"
  interval: 10m
  prune: true
  sourceRef:
    kind: GitRepository
    namespace: flux-system
    name: flux-system
  path: "/manifests/grafana/grafana-helm-release"
  targetNamespace: "telemetry"
  healthChecks:
  - apiVersion: helm.toolkit.fluxcd.io/v2
    kind: HelmRelease
    namespace: "telemetry"
    name: "grafana"
